name: Mirror from source
on:
  repository_dispatch:
    types: [upstream-push]
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
concurrency:
  group: mirror-loyalty-program
  cancel-in-progress: true
jobs:
  mirror:
    if: github.repository == 'yowyob/loyalty-program'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    env:
      UPSTREAM: https://github.com/GAUSS-TPAC/loyalty-program.git
      UPSTREAM_BRANCH: main
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Sync (préserve fichiers de déploiement)
        env:
          MIRROR_PUSH_TOKEN: ${{ secrets.UPSTREAM_TOKEN }}
        run: |
          git config user.name "yowyob-mirror"
          git config user.email "mirror@yowyob.local"
          git remote add upstream "$UPSTREAM"
          git fetch --quiet upstream "$UPSTREAM_BRANCH"
          git checkout upstream/"$UPSTREAM_BRANCH" -- . ':!.github' ':!docker-compose.prod.yml'
          if git diff --cached --quiet; then echo "no change"; exit 0; fi
          git commit -m "mirror: sync https://github.com/GAUSS-TPAC/loyalty-program.git@$UPSTREAM_BRANCH"
          git push "https://x-access-token:${MIRROR_PUSH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" HEAD:main
